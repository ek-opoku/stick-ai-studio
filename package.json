{
  "name": "stick-ai-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI animation workstation architecture.",
  "workspaces": [
    "frontend",
    "renderer",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend,renderer -c cyan,green,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:renderer\"",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:renderer": "npm --workspace renderer run dev",
    "build": "npm --workspace shared run build && npm --workspace frontend run build && npm --workspace renderer run build",
    "render": "npm --workspace renderer run render",
    "render:mp4": "npm --workspace renderer run render:mp4",
    "render:frames": "npm --workspace renderer run render:frames",
    "typecheck": "npm --workspace shared run typecheck && npm --workspace frontend run typecheck && npm --workspace renderer run typecheck",
    "lint": "npm --workspace frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
